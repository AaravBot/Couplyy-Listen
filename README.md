# Couplyy-Listen
SyncSound is a real-time music app where two users listen together in perfect sync. Playback actions are instantly shared, and users joining mid-song are auto-synced to the current timestamp.
